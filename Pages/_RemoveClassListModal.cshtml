@model LetterKnowledgeAssessment.Models.ClassList
@using Microsoft.Extensions.Localization
@inject IStringLocalizerFactory LocalizerFactory

@{
    var Localizer = LocalizerFactory.Create("Strings", "LetterKnowledgeAssessment");
}

<div class="modal fade" id="removeClassListModal-@Model.Id.ToString()">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-dark-primary">
                <h5 class="modal-title light-green">@Localizer["DeleteClass"]</h5>
                <button type="button" class="btn float-end navbar-text-light-green" data-bs-dismiss="modal" aria-label="Close"><i class="bi bi-x-lg"></i></button>
            </div>
            <form asp-area="ClassManagement" asp-page="/Remove" asp-route-classlistid="@Model.Id.ToString()" method="post">
                <div class="modal-body">
                    <div class="text-muted font-weight-lighter" style="font-size: .9rem;">
                        <p>
                            @Localizer["DeleteClassWithStudentsAndTests"]
                            <br /><br />
                            <b>@Localizer["SureAboutDeletion"] @Html.DisplayFor(model => model.Grade) @Localizer["FromAccount"]</b>?
                            <br />
                        </p>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">@Localizer["Cancel"]</button>
                    <button type="submit" class="btn btn-danger">@Localizer["DeleteClass"]</button>
                </div>
            </form>
        </div>
    </div>
</div>